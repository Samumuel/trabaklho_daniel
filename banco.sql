CREATE TABLE pokemon (
    id int AUTO_INCREMENT PRIMARY KEY,
    nome VARCHAR(10),
    descricao VARCHAR(1000),
    link VARCHAR(500),
    tipo1 VARCHAR(2),
    tipo2 VARCHAR(2),
    evolucao VARCHAR(1)
);
